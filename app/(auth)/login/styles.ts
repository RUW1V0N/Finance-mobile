import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1
    }, 
    imageContainer: {
        flex: 0.5,
        width: "100%",
        justifyContent:"center",
        alignItems:"center",
        overflow: "hidden"
    },
    loginButton: {
        padding: 15
    }
})